import { createContext } from "react";

// Create a Context (can be any value: object, string, array, etc.)
export const UserContext = createContext(null);
export const AdminContext = createContext(null);
